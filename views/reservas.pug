extends layout/index

block contenido
    // Reservation Form Section
    section.py-20.bg-white.rounded-xl.shadow-2xl.my-16(data-aos="fade-up")
        .container.mx-auto.px-6
            h2.text-4xl.font-serif.text-vino.mb-6.font-bold Reserva con Nosotros
            p.text-xl.text-gray-700.leading-relaxed.mb-8.font-sans
                | Completa el formulario para reservar una mesa en nuestro restaurante. Nos pondremos en contacto para confirmar tu reserva.
            // Form
            form(action="/reservas" method="POST" class="grid grid-cols-1 md:grid-cols-2 gap-6")
                // Nombre
                .flex.flex-col
                    label(for="nombre" class="text-lg font-medium text-carbon mb-2") Nombre
                    input#nombre(type="text" name="nombre" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-vino")
                // Email
                .flex.flex-col
                    label(for="email" class="text-lg font-medium text-carbon mb-2") Email
                    input#email(type="email" name="email" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-vino")
                // Teléfono
                .flex.flex-col
                    label(for="telefono" class="text-lg font-medium text-carbon mb-2") Teléfono
                    input#telefono(type="tel" name="telefono" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-vino")
                // Fecha de reserva
                .flex.flex-col
                    label(for="fecha_reserva" class="text-lg font-medium text-carbon mb-2") Fecha
                    input#fecha_reserva(type="date" name="fecha_reserva" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-vino")
                //- Hora inicio
                .flex.flex-col
                    label(for="hora_inicio" class="text-lg font-medium text-carbon mb-2") Hora de inicio
                    input#hora_inicio(type="time" name="hora_inicio" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-vino")
                // Número de personas
                .flex.flex-col
                    label(for="numero_personas" class="text-lg font-medium text-carbon mb-2") Número de personas
                    input#numero_personas(type="number" name="numero_personas" min="1" required class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-vino")
                // Canal (oculto, valor "web")
                input(type="hidden" name="canal" value="web")
                // Observaciones
                .flex(class="flex-col md:col-span-2")
                    label(for="observaciones" class="text-lg font-medium text-carbon mb-2") Observaciones (opcional)
                    textarea#observaciones(name="observaciones" rows="4" class="border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-vino")
                // Submit button
                .flex(class="justify-center md:col-span-2")
                    button(type="submit" class="bg-vino text-dorado font-bold py-3 px-8 rounded-full hover:bg-opacity-90 transition transform hover:scale-105")
                        Hacer Reserva
                    
